# Pulse

Pulse is a Python CLI for monitoring HTTP endpoints.

## Current status

The project currently provides the CLI foundation. Endpoint configuration and HTTP health checks will be added in subsequent changes.

## Prerequisites

- Python 3.12 or later
- [uv](https://docs.astral.sh/uv/)

## Setup

```bash
uv sync